// Code generated by goa v3.7.6, DO NOT EDIT.
//
// design HTTP client CLI support package
//
// Command:
// $ goa gen github.com/crossnokaye/carbon/services/baby/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	babynamesc "github.com/crossnokaye/carbon/services/baby/gen/http/baby_names/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `baby-names get-name
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` baby-names get-name --year "Dolorum id eius sapiente reiciendis."` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		babyNamesFlags = flag.NewFlagSet("baby-names", flag.ContinueOnError)

		babyNamesGetNameFlags    = flag.NewFlagSet("get-name", flag.ExitOnError)
		babyNamesGetNameYearFlag = babyNamesGetNameFlags.String("year", "REQUIRED", "year")
	)
	babyNamesFlags.Usage = babyNamesUsage
	babyNamesGetNameFlags.Usage = babyNamesGetNameUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "baby-names":
			svcf = babyNamesFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "baby-names":
			switch epn {
			case "get-name":
				epf = babyNamesGetNameFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "baby-names":
			c := babynamesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get-name":
				endpoint = c.GetName()
				data, err = babynamesc.BuildGetNamePayload(*babyNamesGetNameYearFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// baby-namesUsage displays the usage of the baby-names command and its
// subcommands.
func babyNamesUsage() {
	fmt.Fprintf(os.Stderr, `Service is the BabyNames service interface.
Usage:
    %[1]s [globalflags] baby-names COMMAND [flags]

COMMAND:
    get-name: get most popular baby name

Additional help:
    %[1]s baby-names COMMAND --help
`, os.Args[0])
}
func babyNamesGetNameUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] baby-names get-name -year STRING

get most popular baby name
    -year STRING: year

Example:
    %[1]s baby-names get-name --year "Dolorum id eius sapiente reiciendis."
`, os.Args[0])
}
